const Header = () => {
  return (
    <div>
      <h1>Netlify Serverless Functions Example</h1>

      <p>
        This is a simple example of how we can add serverless functions to our{' '}
        <b>React</b> applications.
      </p>
    </div>
  );
};

export default Header;
